import React from "react";
import Modal from "../Modal";
const StreamDelete = () => {
    return (
        <React.Fragment>
            <div>StreamCrate</div>;
            <Modal />
        </React.Fragment>
    );
};

export default StreamDelete;
